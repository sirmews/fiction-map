id: node-renderer-props-typed
severity: error
message: "Node renderer props must extend NodeRendererProps from @your-org/story-graph-flow"
language: TypeScript
rule:
  pattern: |
    function $NAME(props: $TYPE) {
      $$$BODY
    }
  not:
    regex: "NodeRendererProps|NodeProps"
